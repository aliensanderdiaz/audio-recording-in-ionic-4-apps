import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.aliensanderdiaz.grabarAudio',
  appName: 'Audio Recording',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
